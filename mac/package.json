{
  "name": "jarvis-mac",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Mac-resident vault I/O service. Runs on the machine that holds the Obsidian vault; exposed only over Tailscale.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.0",
    "dotenv": "^16.4.5",
    "hono": "^4.6.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
